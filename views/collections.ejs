<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta http-equiv="X-UA-Compatible" content="IE=edge" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title><%= collection.title %></title>

		<link rel="stylesheet" href="/css/collections.css" />

		<%- include('./includes/links.ejs') %>
	</head>
	<body>
		<%- include('./includes/nav') %> <%- include('./includes/hamnav') %>

		<!-- -------------------------------- Body --------------------------------- -->
		<!-- ------------------------------- Hero Section -------------------------------- -->

		<!-- ------------------------------- Hero Section -------------------------------- -->

		<img
			class="bgimg-artisian"
			src="/<%= collection.bgImage.path %>"
			alt="background image"
		/>

		<section class="hero">
			<h1 class="heading-primary hero__main-heading">
				<span><%= collection.title %></span>
			</h1>
			<img
				src="../images/Scribbles/3.png"
				alt="scribble on screen"
				class="hero__scribble"
			/><a href="#" class="btn-primary hero__btn"
				><span class="btn">EXPLORE</span></a
			>
		</section>

		<!-- -------------------------- about --------------------------- -->
		<section class="artisian">
			<div class="artisian__content">
				<h2 class="artisian__heading">About</h2>
				<h4 class="artisian__subheading"><%= collection.semiDescription %></h4>
				<p class="artisian__para"><%= collection.description %></p>
				<div class="btn-explore">
					<a href="#" class="btn-secondary">EXPLORE</a>
				</div>
			</div>

			<video class="artisian__page-video" autoplay muted loop>
				<source
					src="/images/Compressed/artisian_page/artisian video.mp4"
					type="video/mp4"
				/>
			</video>
		</section>

		<!-- -------------------------------- Items -------------------------------- -->

		<section id="section_items">
			<div class="items">
				<% collection.products.forEach(product => { %>
				<div class="holder">
					<img
						class="bg-color"
						src="/<%= product.images[0].path %>"
						alt="Items"
					/>
					<div class="overlay overlayleft">
						<div class="text">
							<img
								class="transition_img"
								src="/<%= product.images[1].path %>"
								alt="Second image of Items"
							/>
						</div>
					</div>
				</div>
				<% }) %>
			</div>
		</section>

		<%- include('./includes/footer') %>
	</body>
</html>
